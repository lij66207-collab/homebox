# Homebox 家庭库存管理 · 中文使用指南

> 这是一份小白向说明书：照着复制粘贴命令，就能把这套系统搭起来、跑起来、备份好、搬走。
> 本文里的示例 IP 是 `192.168.1.8`，路径是 `/root/github_prj/homebox/homebox`，**换成你自己的即可**。

---

## 一、这是个什么东西

一个自己架在家用服务器上的**家庭物品库存管理**网页应用，手机电脑浏览器都能用。功能：

- 物品/位置/标签管理，购买价格、保修、附件（发票、说明书）
- **AI 拍照识别**：拍张照自动填好名称/描述/数量/标签/位置
- **AI 批量创建**：粘贴一段文字（如"物品A在箱子1，物品B在箱子2"）自动整理入库
- **AI 模糊搜索**：搜"刀子"能找出菜刀、水果刀、瑞士军刀

全家共用一套数据，数据全在你自己服务器上。

---

## 二、你需要准备什么

- 一台一直开着的 Linux 机器（Ubuntu/Debian 都行，虚拟机、旧电脑、云服务器均可），能联网
- 一个 AI 服务的 Key（用于拍照识别等功能），推荐 Kimi for Coding：<https://www.kimi.com> 开通后拿到 `sk-kimi-...` 形式的 key
- 会用 SSH 登录服务器、会复制粘贴

---

## 三、快速搭建（两种方式选一）

### 方式 A：直接用编译好的程序（推荐，最快）

如果你已经有一台跑着的机器（或从本文作者处拿到 `build` 目录），直接把整个 `build` 目录拷到服务器任意位置（比如 `/root/homebox`）即可，**不需要装 Go 和 Node**。跳转到「四、配置开机自启」。

> 注意：二进制区分 CPU 架构，x86 服务器的文件不能拿去 ARM 设备（如树莓派）上跑，那种情况请用方式 B 在目标机器上构建。

### 方式 B：从源码自己构建

```bash
# 1. 安装 Node 22、pnpm、Go 1.26（略，按官网装）
# 2. 拉代码（换成你自己的 fork 地址）
git clone https://github.com/lij66207-collab/homebox.git
cd homebox/frontend
pnpm install
pnpm build                       # 构建前端静态页面（几分钟）

# 3. 把前端产物拷给后端并编译成单个二进制
rm -rf ../backend/app/api/static/public
cp -r .output/public ../backend/app/api/static/public
cd ../backend
CGO_ENABLED=0 go build -o ../build/backend ./app/api
```

编完得到 `build/backend` 这一个文件，就是完整的应用（前端已内嵌）。

---

## 四、配置启动脚本和开机自启

### 1. 创建启动脚本 `build/start.sh`

```bash
#!/bin/bash
cd "$(dirname "$0")"

export HBOX_DATABASE_DRIVER=sqlite3
export HBOX_DATABASE_SQLITE_PATH='.data/homebox.db?_pragma=busy_timeout(1000)&_pragma=journal_mode(WAL)&_pragma=synchronous(NORMAL)&_pragma=foreign_keys(1)&_time_format=sqlite'
export HBOX_OPTIONS_ALLOW_REGISTRATION=true
export HBOX_AUTH_API_KEY_PEPPER="随便一段超过32位的随机字符串"
# 演示/家庭自用可保留下面这行（关闭密码哈希，登录注册更快）；介意就去掉
export UNSAFE_DISABLE_PASSWORD_PROJECTION=yes_i_am_sure
export HBOX_WEB_WRITE_TIMEOUT=180s

# AI 功能（不配就不显示 AI 按钮，其他功能不受影响）
export HBOX_AI_ENABLED=true
export HBOX_AI_API_KEY="你的key"
export HBOX_AI_BASE_URL="https://api.kimi.com/coding/v1"
export HBOX_AI_MODEL="kimi-for-coding"
export HBOX_AI_LANGUAGE="简体中文"
export HBOX_AI_TIMEOUT=120s

exec ./backend
```

```bash
chmod 700 build/start.sh
```

手动测试一下：`./build/start.sh`，看到 `Server is running on :7745` 就 OK，`Ctrl+C` 停掉。

### 2. 注册为系统服务（开机自启 + 崩溃自愈）

新建 `/etc/systemd/system/homebox.service`：

```ini
[Unit]
Description=Homebox home inventory server
After=network-online.target
Wants=network-online.target

[Service]
Type=simple
WorkingDirectory=/root/homebox/build
ExecStart=/root/homebox/build/start.sh
Restart=always
RestartSec=3

[Install]
WantedBy=multi-user.target
```

> 路径改成你实际放 build 的位置。

```bash
systemctl daemon-reload
systemctl enable --now homebox
systemctl status homebox        # 看到 active (running) 即成功
```

### 3. 访问

- 浏览器打开 `http://服务器IP:7745`，注册第一个账号（注册按钮灰着是密码强度不够，用 8 位以上字母+数字混合密码）
- 局域网访问不到 → 检查防火墙：`ufw allow 7745`（云服务器还要改安全组）

---

## 五、HTTPS（手机扫码/相机功能需要）

手机的**相机扫码**功能要求网页必须是 HTTPS。局域网自签名方案（用项目里的 Caddy）：

新建 `build/Caddyfile`（IP 换成你的）：

```
{
	auto_https disable_redirects
}

https://192.168.1.8:8443 {
	tls internal
	reverse_proxy http://localhost:7745
}
```

新建 `/etc/systemd/system/homebox-caddy.service`：

```ini
[Unit]
Description=Caddy HTTPS reverse proxy for Homebox
After=network-online.target homebox.service
Wants=network-online.target

[Service]
Type=simple
Environment=XDG_DATA_HOME=/root/homebox/build/.caddy-data
ExecStart=/root/homebox/build/caddy run --config /root/homebox/build/Caddyfile
Restart=always
RestartSec=3

[Install]
WantedBy=multi-user.target
```

```bash
systemctl daemon-reload && systemctl enable --now homebox-caddy
```

访问 `https://服务器IP:8443`，首次有证书警告，点「继续前往」即可；想消除警告，把 `build/.caddy-data/caddy/pki/authorities/local/root.crt` 传到手机安装为受信任根证书。

---

## 六、数据备份

**所有数据都在 `build/.data/` 一个目录里**（数据库 `homebox.db` + 照片等附件）。备份 = 打包这个目录。

### 手动备份

```bash
systemctl stop homebox
tar -czf homebox-backup-$(date +%Y%m%d).tar.gz -C /root/homebox/build .data
systemctl start homebox
```

### 自动每日备份（保留最近 14 天）

新建 `/root/backup-homebox.sh`：

```bash
#!/bin/bash
DEST=/root/backups
mkdir -p "$DEST"
systemctl stop homebox
tar -czf "$DEST/homebox-$(date +%Y%m%d-%H%M).tar.gz" -C /root/homebox/build .data
systemctl start homebox
find "$DEST" -name 'homebox-*.tar.gz' -mtime +14 -delete
```

```bash
chmod +x /root/backup-homebox.sh
# 加入 crontab：crontab -e，添加一行（每天凌晨 3:25 执行）
25 3 * * * /root/backup-homebox.sh >> /var/log/homebox-backup.log 2>&1
```

> 重要：备份文件要**定期拷到别的机器/U盘/网盘**（`scp` 或 `rsync` 都行），只留在本机等于没备份。

---

## 七、迁移到另一台服务器

整个应用 = `build` 目录（二进制 + 数据 + 启动脚本 + Caddyfile），搬走就是拷目录：

```bash
# 老服务器上：
systemctl stop homebox homebox-caddy
tar -czf homebox-move.tar.gz -C /root/homebox build
scp homebox-move.tar.gz 新服务器IP:/root/

# 新服务器上：
mkdir -p /root/homebox
tar -xzf /root/homebox-move.tar.gz -C /root/homebox
# 按第四节创建两个 systemd 服务（路径对应改一下）
# Caddyfile 里的 IP 改成新服务器的 IP
systemctl daemon-reload
systemctl enable --now homebox homebox-caddy
```

检查清单：

1. 两台机器 **CPU 架构一致**（都是 x86_64 就能直接搬；不一致要在新机器上按方式 B 重新构建，`.data` 目录照常拷贝）
2. `start.sh` 里的路径是相对路径，不用改
3. `Caddyfile` 里的 **IP 要改成新服务器的**
4. 防火墙放行 7745 / 8443
5. 启动后先登录试一次，再跑一下 AI 识别确认 key 正常

---

## 八、日常运维速查

| 操作 | 命令 |
|---|---|
| 看状态 | `systemctl status homebox homebox-caddy` |
| 重启 | `systemctl restart homebox` |
| 看日志 | `journalctl -u homebox -f` |
| 停止 | `systemctl stop homebox` |
| 改配置 | 编辑 `build/start.sh`，然后 `systemctl restart homebox` |

## 九、常见问题

- **注册按钮灰着点不动**：密码强度不够。用 8 位以上、字母+数字混合的密码
- **AI 识别失败**：检查 `start.sh` 里的 key 和 `HBOX_AI_BASE_URL` 是否配对（Kimi for Coding 的 key 要配 `https://api.kimi.com/coding/v1`）；用 `journalctl -u homebox -f` 看具体报错
- **页面能开但相机调不起来**：必须用 HTTPS（第五节）
- **换服务器后登录不进去**：确认整个 `.data` 目录都拷过来了，且服务已重启
